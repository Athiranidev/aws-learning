# Commands

## Apache Setup

sudo systemctl start httpd
→ Starts Apache web server

sudo systemctl enable httpd
→ Enables auto-start on reboot

sudo nano /var/www/html/index.html
→ Used to modify website content

curl localhost
→ Verifies web server is running locally
